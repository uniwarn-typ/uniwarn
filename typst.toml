[package]
name = "font-warnings"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["zral0kh", "OrangeX4"]
license = "MIT"
description = "A miniscule package for package authors to emit custom warnings."
repository = "https://github.com/fornings-typ/font-warnings"
keywords = ["warning", "illegal"]
categories = ["utility"]
exclude = [tests]
compiler = "0.12.4"

[tool.tytanic]
default.ppi = 72